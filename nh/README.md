# nh.helixa.xyz

This directory is reserved as the repo path for the `nh.helixa.xyz` subdomain.

Suggested uses:
- static site root
- standalone app mounted behind nginx
- placeholder build target before wiring a local dev port

Current starter files:
- `index.html` - minimal placeholder page

Server-side wiring can point `nh.helixa.xyz` here directly, or proxy to a separate app later.
